"use client";
import { ChangeEvent, useState } from "react";
import Link from "next/link";
function parseCSV(text:string){const lines=text.split(/\r?\n/).filter(Boolean);if(!lines.length)return[];const headers=lines[0].split(",").map(x=>x.trim().toLowerCase());return lines.slice(1).map(line=>{const values=line.split(",");return Object.fromEntries(headers.map((h,i)=>[h,(values[i]??"").trim()]))})}
export default function ImportPage(){const[file,setFile]=useState<File|null>(null),[rows,setRows]=useState<any[]>([]),[result,setResult]=useState<any>(null),[busy,setBusy]=useState(false);async function upload(){if(!file)return;setBusy(true);setResult(null);try{const parsed=parseCSV(await file.text());setRows(parsed);const leads=parsed.map(r=>({name:r.name||r.full_name||r.customer_name,phone:r.phone||r.mobile||r.whatsapp,consent:r.consent,source:r.source||"CSV_IMPORT"}));const res=await fetch("/api/leads/import",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({leads})});setResult(await res.json())}catch{setResult({error:"Upload failed"})}finally{setBusy(false)}}return <main className="shell"><header className="top"><div><div className="brand">Import Leads</div><div className="label">Bulk customer database import</div></div><Link className="btn secondary" href="/leads">Lead Database</Link></header><section className="card section"><h2>Upload CSV</h2><p className="label">Columns supported: <b>name, phone/mobile/whatsapp, consent, source</b>.</p><input type="file" accept=".csv,text/csv" onChange={(e:ChangeEvent<HTMLInputElement>)=>setFile(e.target.files?.[0]??null)}/>{file&&<p className="label">Selected: {file.name}</p>}<div className="actions" style={{marginTop:16}}><button className="btn" disabled={!file||busy} onClick={upload}>{busy?"Importing…":"Import leads"}</button></div>{result&&<pre className="result">{JSON.stringify(result,null,2)}</pre>}</section><section className="card section"><h2>Example CSV</h2><pre className="result">name,phone,consent,source{`\n`}Rahul,+919876543210,true,Facebook Lead Ad{`\n`}Priya,+919812345678,false,Instagram Lead Ad</pre></section><section className="card section"><b>Safety:</b> Imported leads remain ineligible for WhatsApp marketing unless explicit consent is recorded, and opted-out customers are excluded from campaigns.</section></main>}
